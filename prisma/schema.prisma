generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  ADMIN
  REVIEWER
  CUSTOMER
}

enum SupplierTier {
  TIER_1
  TIER_2
  TIER_3
  OTHER
}

enum Criticality {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum TransportMode {
  ROAD
  RAIL
  SEA
  AIR
  MULTIMODAL
  OTHER
}

enum SourceType {
  RSS
  ATOM
  API
  WEB
  MANUAL
}

enum SourceCategory {
  NEWS
  LOCAL_NEWS
  GOVERNMENT
  REGULATOR
  INDUSTRY
  PORT
  LOGISTICS
  LABOUR
  TRADE
  WEATHER
  MARKET
  SUPPLIER
  OTHER
}

enum SourceReliability {
  LOW
  MEDIUM
  HIGH
  PRIMARY
}

enum SourceArticleStatus {
  COLLECTED
  NORMALIZED
  FAILED
  IGNORED
}

enum CollectionRunStatus {
  RUNNING
  SUCCESS
  PARTIAL
  FAILED
}

enum NewsRadarTopic {
  GEOPOLITICAL
  ECONOMIC
  OPERATIONAL
  LOGISTICS
  ENVIRONMENTAL
  TRADE
  TECHNOLOGY
}

enum NewsRadarEntityType {
  SUPPLIER
  FACTORY
  PRODUCT
  MATERIAL
  ROUTE
  PORT
}

enum NewsRadarMatchMethod {
  EXACT_LEGAL_NAME
  UNIQUE_EXACT_NAME
  NAME_AND_LOCATION
  EXACT_CITY_COUNTRY
  EXACT_COUNTRY
  EXACT_PORT_NAME
  EXACT_PORT_CODE
  ROUTE_ENDPOINTS
  INDUSTRY_CONTEXT
}

enum NewsRadarProcessingStatus {
  RUNNING
  COMPLETED
  FAILED
}

enum NewsRadarRelevanceLevel {
  HIGH
  MEDIUM
  LOW
}

enum PocIngestionMode {
  INITIAL_FULL_LOAD
  DELTA
  DAILY_RECONCILIATION
}

enum PocIngestionRunStatus {
  RUNNING
  COMPLETED
  PARTIAL
  FAILED
}

enum MonitoringTagType {
  AUTO
  SUGGESTED
  CUSTOM
}

enum MonitoringTagStatus {
  PENDING
  ACTIVE
  DISABLED
  IGNORED
}

enum MonitoringTagCategory {
  SUPPLIER
  FACTORY
  PRODUCT
  MATERIAL
  LOCATION
  COUNTRY
  REGION
  PORT
  ROUTE
  INDUSTRY
  THEME
}

enum ArticleTranslationStatus {
  PENDING
  COMPLETED
  FAILED
}

enum DailyBriefDeliveryStatus {
  PENDING
  SENT
  FAILED
}

enum ExtractionRunStatus {
  PROCESSING
  COMPLETED
  PARTIAL
  FAILED
}

enum ClaimType {
  LABOUR_DISRUPTION
  STRIKE
  FACTORY_DISRUPTION
  FACTORY_CLOSURE
  PORT_DISRUPTION
  LOGISTICS_DISRUPTION
  TRANSPORT_DISRUPTION
  WEATHER_DISRUPTION
  NATURAL_HAZARD
  FIRE
  FLOOD
  EARTHQUAKE
  CYCLONE
  TYPHOON
  POLITICAL_DISRUPTION
  CIVIL_UNREST
  TRADE_RESTRICTION
  IMPORT_RESTRICTION
  EXPORT_RESTRICTION
  SANCTION
  REGULATORY_CHANGE
  CUSTOMS_CHANGE
  TARIFF_CHANGE
  MATERIAL_SHORTAGE
  ENERGY_DISRUPTION
  INFRASTRUCTURE_DISRUPTION
  SECURITY_INCIDENT
  OTHER
}

enum AssertionMode {
  OBSERVED
  REPORTED
  ANNOUNCED
  FORECAST
  PLANNED
  ESTIMATED
}

enum ClaimEntityType {
  ORGANIZATION
  COMPANY
  GOVERNMENT
  REGULATOR
  INDUSTRY_ASSOCIATION
  LABOUR_UNION
  FACTORY
  PORT
  AIRPORT
  CITY
  REGION
  COUNTRY
  PRODUCT
  MATERIAL
  COMMODITY
  TRANSPORT_ROUTE
  OTHER
}

enum PocDatasetStatus {
  DRAFT
  RUNNING
  REVIEWING
  COMPLETED
}

enum PocArticleStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

enum EvaluationScore {
  CORRECT
  PARTIALLY_CORRECT
  INCORRECT
  NOT_APPLICABLE
}

enum EvaluationFailureReason {
  HALLUCINATION
  MISINTERPRETATION
  NEGATION_ERROR
  FORECAST_OBSERVED_ERROR
  DATE_ERROR
  ENTITY_ERROR
  LOCATION_ERROR
  CLAIM_TOO_BROAD
  CLAIM_TOO_FRAGMENTED
  MISSING_CLAIM
  IRRELEVANT_CLAIM
  INSUFFICIENT_ARTICLE_TEXT
  TRUNCATION
  SOURCE_AMBIGUITY
  OTHER
}

enum EventType {
  FACTORY_DISRUPTION
  FACTORY_CLOSURE
  FACTORY_EXPANSION
  PRODUCTION_REDUCTION
  PRODUCTION_INCREASE
  PORT_DISRUPTION
  PORT_CLOSURE
  PORT_CONGESTION
  SHIPPING_DISRUPTION
  LOGISTICS_DISRUPTION
  STRIKE
  LABOR_DISRUPTION
  NATURAL_HAZARD
  WEATHER_DISRUPTION
  FIRE
  EXPLOSION
  ACCIDENT
  POWER_OUTAGE
  CYBER_INCIDENT
  REGULATORY_CHANGE
  TRADE_RESTRICTION
  SANCTION
  TARIFF_CHANGE
  EXPORT_RESTRICTION
  IMPORT_RESTRICTION
  SUPPLIER_DISRUPTION
  MATERIAL_SHORTAGE
  CAPACITY_CHANGE
  OTHER
}

enum EventStatus {
  DETECTED
  ACTIVE
  RESOLVED
  CANCELLED
}

enum EventSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum EventEntityType {
  SUPPLIER
  COMPANY
  FACTORY
  PORT
  AIRPORT
  WAREHOUSE
  DISTRIBUTION_CENTER
  MATERIAL
  PRODUCT
  TRANSPORT_ROUTE
  VESSEL
  REGION
  COUNTRY
  OTHER
}

enum EventLocationType {
  CITY
  REGION
  COUNTRY
  PORT
  FACTORY
  OTHER
}

enum EventTemporalPrecision {
  UNKNOWN
  DAY
  RANGE
}

enum EventConflictState {
  NONE
  DETECTED
}

enum EventMatchDecision {
  CREATE_NEW
  MATCH_EXISTING
  AMBIGUOUS
}

enum ClaimSignal {
  AFFIRMS_EVENT
  DENIES_EVENT
  RESOLUTION_SIGNAL
  CANCELLATION_SIGNAL
  NEUTRAL
}

enum ClaimEventProcessingStatus {
  PENDING
  PROCESSING
  COMPLETED
  SKIPPED
  FAILED
}

enum ExposureType {
  DIRECT_SUPPLIER
  DIRECT_FACTORY
  DIRECT_PORT
  DIRECT_ROUTE
  DIRECT_MATERIAL
  DIRECT_PRODUCT
  INDIRECT_SUPPLIER
  INDIRECT_FACTORY
  INDIRECT_ROUTE
  GEOGRAPHIC_PROXIMITY
}

enum ExposureStatus {
  POTENTIAL
  CONFIRMED
  DISMISSED
  STALE
  RESOLVED
}

enum ExposureDecision {
  MATCH
  AMBIGUOUS
  NO_MATCH
}

enum ExposureMatchMethod {
  VERIFIED_IDENTIFIER
  REVIEWED_IDENTITY_MAPPING
  GLOBAL_PORT_ID
  PORT_CODE
  COMPOSITE_EXACT_IDENTITY
  EXACT_STRUCTURED_LOCATION
  COORDINATE_PROXIMITY
  EXPLICIT_GRAPH_PATH
}

enum ExposureMatchState {
  VERIFIED_DIRECT
  VERIFIED_INDIRECT
  GEOGRAPHIC
  AMBIGUOUS
}

enum ExposureNodeType {
  SUPPLIER
  FACTORY
  PRODUCT
  MATERIAL
  ROUTE
  PORT
}

enum IdentitySubjectType {
  SUPPLIER
  FACTORY
  PRODUCT
  MATERIAL
  ROUTE
  PORT
}

enum IdentityVerificationStatus {
  PROPOSED
  VERIFIED
  UNVERIFIED
  REJECTED
}

enum ExposureCandidateStatus {
  PENDING
  CONFIRMED
  REJECTED
  SUPERSEDED
}

enum ExposureReasonCode {
  EXACT_SUPPLIER_IDENTIFIER
  EXACT_FACTORY_IDENTIFIER
  EXACT_PRODUCT_IDENTIFIER
  EXACT_MATERIAL_IDENTIFIER
  EXACT_ROUTE_IDENTIFIER
  EXACT_PORT_ID
  EXACT_PORT_CODE
  COMPOSITE_SUPPLIER_IDENTITY
  COMPOSITE_FACTORY_IDENTITY
  ROUTE_CONTAINS_ORIGIN_PORT
  ROUTE_CONTAINS_DESTINATION_PORT
  ROUTE_CONTAINS_INTERMEDIATE_PORT
  EXPLICIT_SUPPLIER_PRODUCT_PATH
  EXPLICIT_FACTORY_PRODUCT_PATH
  EXPLICIT_PRODUCT_MATERIAL_PATH
  EXPLICIT_ROUTE_SUPPLIER_PATH
  EXPLICIT_ROUTE_FACTORY_PATH
  EXACT_COORDINATE_PROXIMITY
  EXACT_CITY_REGION_COUNTRY
  EXACT_REGION_COUNTRY
  COUNTRY_ONLY_PROXIMITY
  AMBIGUOUS_ENTITY_IDENTITY
  AMBIGUOUS_LOCATION
  NAME_ONLY_MATCH_REJECTED
  COUNTRY_MISMATCH
  PORT_COUNTRY_MISMATCH
  FACTORY_NOT_IN_CUSTOMER_GRAPH
  NO_EXPLICIT_GRAPH_PATH
  NODE_ARCHIVED
  NO_SUPPORTED_MATCH
}

model User {
  id                             String                  @id @db.Uuid
  email                          String                  @unique
  name                           String?
  role                           UserRole                @default(CUSTOMER)
  memberships                    CustomerMembership[]
  claimEvaluations               ClaimEvaluation[]
  articleEvaluations             ArticleEvaluation[]
  reviewedExposures              CustomerExposure[]
  verifiedGraphIdentities        CustomerGraphIdentity[]
  proposedEventEntityIdentifiers EventEntityIdentifier[] @relation("EventEntityIdentifierProposer")
  verifiedEventEntityIdentifiers EventEntityIdentifier[] @relation("EventEntityIdentifierVerifier")
  reviewedExposureCandidates     ExposureCandidate[]
  dailyBriefPreferences          DailyBriefPreference[]
  dailyBriefDeliveries           DailyBriefDelivery[]
  monitoringTags                 CustomerMonitoringTag[] @relation("MonitoringTagCreator")
  createdAt                      DateTime                @default(now()) @map("created_at")
  updatedAt                      DateTime                @updatedAt @map("updated_at")

  @@map("users")
}

model Customer {
  id                    String                     @id @default(uuid()) @db.Uuid
  name                  String
  description           String?
  defaultAlertThreshold Int                        @default(60) @map("default_alert_threshold")
  memberships           CustomerMembership[]
  suppliers             Supplier[]
  companies             Company[]
  locations             Location[]
  companySuppliers      CompanySupplier[]
  factoryLocations      FactoryLocation[]
  factories             Factory[]
  products              Product[]
  materials             Material[]
  routes                Route[]
  graphRevision         BigInt                     @default(0) @map("graph_revision")
  exposures             CustomerExposure[]
  exposureCandidates    ExposureCandidate[]
  graphIdentities       CustomerGraphIdentity[]
  newsRadarExposures    NewsRadarExposure[]
  dailyBriefPreferences DailyBriefPreference[]
  dailyBriefDeliveries  DailyBriefDelivery[]
  dailyBriefs           DailyBrief[]
  monitoringTags        CustomerMonitoringTag[]
  sourcePreferences     CustomerSourcePreference[]
  createdAt             DateTime                   @default(now()) @map("created_at")
  updatedAt             DateTime                   @updatedAt @map("updated_at")

  @@map("customers")
}

model Company {
  id               String            @id @default(uuid()) @db.Uuid
  customerId       String            @map("customer_id") @db.Uuid
  customer         Customer          @relation(fields: [customerId], references: [id], onDelete: Restrict)
  name             String
  legalName        String?           @map("legal_name")
  country          String?
  location         String?
  category         String?
  sourceName       String            @map("source_name")
  sourceUrl        String            @map("source_url")
  verifiedAt       DateTime          @map("verified_at")
  active           Boolean           @default(true)
  companySuppliers CompanySupplier[]
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@unique([customerId, name])
  @@index([customerId, active])
  @@map("companies")
}

model Country {
  id         String     @id @default(uuid()) @db.Uuid
  name       String     @unique
  iso2       String     @unique @db.Char(2)
  sourceName String     @map("source_name")
  sourceUrl  String     @map("source_url")
  verifiedAt DateTime   @map("verified_at")
  locations  Location[]
  createdAt  DateTime   @default(now()) @map("created_at")
  updatedAt  DateTime   @updatedAt @map("updated_at")

  @@map("countries")
}

model Location {
  id               String            @id @default(uuid()) @db.Uuid
  customerId       String            @map("customer_id") @db.Uuid
  customer         Customer          @relation(fields: [customerId], references: [id], onDelete: Restrict)
  countryId        String            @map("country_id") @db.Uuid
  countryRef       Country           @relation(fields: [countryId], references: [id], onDelete: Restrict)
  name             String
  country          String
  location         String?
  category         String?
  latitude         Decimal?          @db.Decimal(9, 6)
  longitude        Decimal?          @db.Decimal(9, 6)
  sourceName       String            @map("source_name")
  sourceUrl        String            @map("source_url")
  verifiedAt       DateTime          @map("verified_at")
  active           Boolean           @default(true)
  factoryLocations FactoryLocation[]
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@unique([customerId, name, countryId])
  @@index([customerId, active])
  @@map("locations")
}

model CustomerMembership {
  id                   String                @id @default(uuid()) @db.Uuid
  userId               String                @map("user_id") @db.Uuid
  customerId           String                @map("customer_id") @db.Uuid
  user                 User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  customer             Customer              @relation(fields: [customerId], references: [id], onDelete: Cascade)
  dailyBriefPreference DailyBriefPreference?
  createdAt            DateTime              @default(now()) @map("created_at")
  updatedAt            DateTime              @updatedAt @map("updated_at")

  @@unique([userId, customerId])
  @@index([customerId])
  @@map("customer_memberships")
}

model Supplier {
  id                     String                  @id @default(uuid()) @db.Uuid
  customerId             String                  @map("customer_id") @db.Uuid
  customer               Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  name                   String
  legalName              String?                 @map("legal_name")
  country                String
  city                   String?
  latitude               Decimal?                @db.Decimal(9, 6)
  longitude              Decimal?                @db.Decimal(9, 6)
  supplierType           String?                 @map("supplier_type")
  category               String?
  sourceName             String?                 @map("source_name")
  sourceUrl              String?                 @map("source_url")
  verifiedAt             DateTime?               @map("verified_at")
  tier                   SupplierTier
  criticality            Criticality
  active                 Boolean                 @default(true)
  factories              Factory[]
  supplierProducts       SupplierProduct[]
  routeSuppliers         RouteSupplier[]
  exposurePathSteps      ExposurePathStep[]
  exposureCandidateNodes ExposureCandidateNode[]
  graphIdentities        CustomerGraphIdentity[]
  newsRadarExposures     NewsRadarExposure[]
  companySuppliers       CompanySupplier[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@index([customerId, active])
  @@index([customerId, country])
  @@map("suppliers")
}

model Factory {
  id                          String                  @id @default(uuid()) @db.Uuid
  customerId                  String                  @map("customer_id") @db.Uuid
  customer                    Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  supplierId                  String?                 @map("supplier_id") @db.Uuid
  supplier                    Supplier?               @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  name                        String
  address                     String?
  country                     String
  city                        String?
  latitude                    Decimal?                @db.Decimal(9, 6)
  longitude                   Decimal?                @db.Decimal(9, 6)
  productionType              String?                 @map("production_type")
  category                    String?
  sourceName                  String?                 @map("source_name")
  sourceUrl                   String?                 @map("source_url")
  verifiedAt                  DateTime?               @map("verified_at")
  supplierRelationSourceName  String?                 @map("supplier_relation_source_name")
  supplierRelationSourceUrl   String?                 @map("supplier_relation_source_url")
  supplierRelationCollectedAt DateTime?               @map("supplier_relation_collected_at")
  supplierRelationConfidence  Decimal?                @map("supplier_relation_confidence") @db.Decimal(4, 3)
  criticality                 Criticality
  active                      Boolean                 @default(true)
  factoryProducts             FactoryProduct[]
  routeFactories              RouteFactory[]
  exposurePathSteps           ExposurePathStep[]
  exposureCandidateNodes      ExposureCandidateNode[]
  graphIdentities             CustomerGraphIdentity[]
  newsRadarExposures          NewsRadarExposure[]
  factoryLocations            FactoryLocation[]
  createdAt                   DateTime                @default(now()) @map("created_at")
  updatedAt                   DateTime                @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@index([customerId, active])
  @@index([customerId, supplierId])
  @@map("factories")
}

model Product {
  id                     String                  @id @default(uuid()) @db.Uuid
  customerId             String                  @map("customer_id") @db.Uuid
  customer               Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  name                   String
  sku                    String?
  category               String?
  description            String?
  country                String?
  location               String?
  sourceName             String?                 @map("source_name")
  sourceUrl              String?                 @map("source_url")
  verifiedAt             DateTime?               @map("verified_at")
  criticality            Criticality
  active                 Boolean                 @default(true)
  factoryProducts        FactoryProduct[]
  productMaterials       ProductMaterial[]
  supplierProducts       SupplierProduct[]
  exposurePathSteps      ExposurePathStep[]
  exposureCandidateNodes ExposureCandidateNode[]
  graphIdentities        CustomerGraphIdentity[]
  newsRadarExposures     NewsRadarExposure[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@unique([customerId, sku])
  @@index([customerId, active])
  @@map("products")
}

model Material {
  id                     String                  @id @default(uuid()) @db.Uuid
  customerId             String                  @map("customer_id") @db.Uuid
  customer               Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  name                   String
  category               String?
  commodity              String?
  country                String?
  location               String?
  sourceName             String?                 @map("source_name")
  sourceUrl              String?                 @map("source_url")
  verifiedAt             DateTime?               @map("verified_at")
  criticality            Criticality
  substitutable          Boolean                 @default(false)
  active                 Boolean                 @default(true)
  productMaterials       ProductMaterial[]
  exposurePathSteps      ExposurePathStep[]
  exposureCandidateNodes ExposureCandidateNode[]
  graphIdentities        CustomerGraphIdentity[]
  newsRadarExposures     NewsRadarExposure[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@index([customerId, active])
  @@map("materials")
}

model Port {
  id                     String                  @id @default(uuid()) @db.Uuid
  name                   String
  country                String
  city                   String?
  latitude               Decimal?                @db.Decimal(9, 6)
  longitude              Decimal?                @db.Decimal(9, 6)
  portCode               String?                 @unique @map("port_code")
  location               String?
  category               String?
  sourceName             String?                 @map("source_name")
  sourceUrl              String?                 @map("source_url")
  verifiedAt             DateTime?               @map("verified_at")
  active                 Boolean                 @default(true)
  routePorts             RoutePort[]
  exposurePathSteps      ExposurePathStep[]
  exposureCandidateNodes ExposureCandidateNode[]
  graphIdentities        CustomerGraphIdentity[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@index([active, country])
  @@map("ports")
}

model Route {
  id                     String                  @id @default(uuid()) @db.Uuid
  customerId             String                  @map("customer_id") @db.Uuid
  customer               Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  name                   String
  originLabel            String                  @map("origin_label")
  destinationLabel       String                  @map("destination_label")
  transportMode          TransportMode           @map("transport_mode")
  country                String?
  location               String?
  category               String?
  sourceName             String?                 @map("source_name")
  sourceUrl              String?                 @map("source_url")
  verifiedAt             DateTime?               @map("verified_at")
  criticality            Criticality
  active                 Boolean                 @default(true)
  routePorts             RoutePort[]
  routeSuppliers         RouteSupplier[]
  routeFactories         RouteFactory[]
  exposurePathSteps      ExposurePathStep[]
  exposureCandidateNodes ExposureCandidateNode[]
  graphIdentities        CustomerGraphIdentity[]
  newsRadarExposures     NewsRadarExposure[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@index([customerId, active])
  @@map("routes")
}

model FactoryProduct {
  customerId  String    @map("customer_id") @db.Uuid
  factoryId   String    @map("factory_id") @db.Uuid
  productId   String    @map("product_id") @db.Uuid
  factory     Factory   @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  product     Product   @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt   DateTime  @default(now()) @map("created_at")
  sourceName  String?   @map("source_name")
  sourceUrl   String?   @map("source_url")
  collectedAt DateTime? @map("collected_at")
  confidence  Decimal?  @db.Decimal(4, 3)

  @@id([factoryId, productId])
  @@index([customerId])
  @@map("factory_products")
}

model ProductMaterial {
  customerId  String    @map("customer_id") @db.Uuid
  productId   String    @map("product_id") @db.Uuid
  materialId  String    @map("material_id") @db.Uuid
  product     Product   @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  material    Material  @relation(fields: [materialId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt   DateTime  @default(now()) @map("created_at")
  sourceName  String?   @map("source_name")
  sourceUrl   String?   @map("source_url")
  collectedAt DateTime? @map("collected_at")
  confidence  Decimal?  @db.Decimal(4, 3)

  @@id([productId, materialId])
  @@index([customerId])
  @@map("product_materials")
}

model SupplierProduct {
  customerId  String    @map("customer_id") @db.Uuid
  supplierId  String    @map("supplier_id") @db.Uuid
  productId   String    @map("product_id") @db.Uuid
  supplier    Supplier  @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  product     Product   @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt   DateTime  @default(now()) @map("created_at")
  sourceName  String?   @map("source_name")
  sourceUrl   String?   @map("source_url")
  collectedAt DateTime? @map("collected_at")
  confidence  Decimal?  @db.Decimal(4, 3)

  @@id([supplierId, productId])
  @@index([customerId])
  @@map("supplier_products")
}

model RoutePort {
  customerId         String              @map("customer_id") @db.Uuid
  routeId            String              @map("route_id") @db.Uuid
  portId             String              @map("port_id") @db.Uuid
  sequence           Int
  route              Route               @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  port               Port                @relation(fields: [portId], references: [id], onDelete: Restrict)
  createdAt          DateTime            @default(now()) @map("created_at")
  sourceName         String?             @map("source_name")
  sourceUrl          String?             @map("source_url")
  collectedAt        DateTime?           @map("collected_at")
  confidence         Decimal?            @db.Decimal(4, 3)
  newsRadarExposures NewsRadarExposure[]

  @@id([routeId, portId])
  @@unique([routeId, portId, customerId])
  @@unique([routeId, sequence])
  @@index([customerId])
  @@index([portId])
  @@map("route_ports")
}

model RouteSupplier {
  customerId  String    @map("customer_id") @db.Uuid
  routeId     String    @map("route_id") @db.Uuid
  supplierId  String    @map("supplier_id") @db.Uuid
  route       Route     @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  supplier    Supplier  @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt   DateTime  @default(now()) @map("created_at")
  sourceName  String?   @map("source_name")
  sourceUrl   String?   @map("source_url")
  collectedAt DateTime? @map("collected_at")
  confidence  Decimal?  @db.Decimal(4, 3)

  @@id([routeId, supplierId])
  @@index([customerId])
  @@map("route_suppliers")
}

model RouteFactory {
  customerId  String    @map("customer_id") @db.Uuid
  routeId     String    @map("route_id") @db.Uuid
  factoryId   String    @map("factory_id") @db.Uuid
  route       Route     @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  factory     Factory   @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt   DateTime  @default(now()) @map("created_at")
  sourceName  String?   @map("source_name")
  sourceUrl   String?   @map("source_url")
  collectedAt DateTime? @map("collected_at")
  confidence  Decimal?  @db.Decimal(4, 3)

  @@id([routeId, factoryId])
  @@index([customerId])
  @@map("route_factories")
}

model CompanySupplier {
  customerId  String   @map("customer_id") @db.Uuid
  companyId   String   @map("company_id") @db.Uuid
  supplierId  String   @map("supplier_id") @db.Uuid
  customer    Customer @relation(fields: [customerId], references: [id], onDelete: Restrict)
  company     Company  @relation(fields: [companyId, customerId], references: [id, customerId], onDelete: Restrict)
  supplier    Supplier @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  sourceName  String   @map("source_name")
  sourceUrl   String   @map("source_url")
  collectedAt DateTime @map("collected_at")
  confidence  Decimal  @db.Decimal(4, 3)
  createdAt   DateTime @default(now()) @map("created_at")

  @@id([companyId, supplierId])
  @@index([customerId])
  @@map("company_suppliers")
}

model FactoryLocation {
  customerId  String   @map("customer_id") @db.Uuid
  factoryId   String   @map("factory_id") @db.Uuid
  locationId  String   @map("location_id") @db.Uuid
  customer    Customer @relation(fields: [customerId], references: [id], onDelete: Restrict)
  factory     Factory  @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  location    Location @relation(fields: [locationId, customerId], references: [id, customerId], onDelete: Restrict)
  sourceName  String   @map("source_name")
  sourceUrl   String   @map("source_url")
  collectedAt DateTime @map("collected_at")
  confidence  Decimal  @db.Decimal(4, 3)
  createdAt   DateTime @default(now()) @map("created_at")

  @@id([factoryId, locationId])
  @@index([customerId])
  @@map("factory_locations")
}

model Source {
  id                         String                     @id @default(uuid()) @db.Uuid
  name                       String
  sourceType                 SourceType                 @map("source_type")
  baseUrl                    String                     @map("base_url")
  feedUrl                    String?                    @map("feed_url")
  country                    String?
  region                     String?
  industry                   String?
  language                   String?
  category                   SourceCategory
  reliability                SourceReliability
  active                     Boolean                    @default(true)
  collectionEnabled          Boolean                    @default(false) @map("collection_enabled")
  collectionIntervalMinutes  Int?                       @default(15) @map("collection_interval_minutes")
  lastCollectedAt            DateTime?                  @map("last_collected_at")
  lastSuccessfulCollectionAt DateTime?                  @map("last_successful_collection_at")
  lastFailureAt              DateTime?                  @map("last_failure_at")
  consecutiveFailures        Int                        @default(0) @map("consecutive_failures")
  articles                   SourceArticle[]
  collectionRuns             SourceCollectionRun[]
  customerPreferences        CustomerSourcePreference[]
  createdAt                  DateTime                   @default(now()) @map("created_at")
  updatedAt                  DateTime                   @updatedAt @map("updated_at")

  @@index([active, collectionEnabled])
  @@index([sourceType, category])
  @@map("sources")
}

model PocIngestionState {
  id                       String                 @id @default("POC_V1")
  ownerToken               String?                @map("owner_token") @db.Uuid
  leaseExpiresAt           DateTime?              @map("lease_expires_at")
  lastRunStartedAt         DateTime?              @map("last_run_started_at")
  lastRunCompletedAt       DateTime?              @map("last_run_completed_at")
  lastRunStatus            PocIngestionRunStatus? @map("last_run_status")
  lastFullLoadAt           DateTime?              @map("last_full_load_at")
  lastSuccessfulDeltaAt    DateTime?              @map("last_successful_delta_at")
  lastFullReconciliationAt DateTime?              @map("last_full_reconciliation_at")
  pendingBacklog           Int                    @default(0) @map("pending_backlog")
  createdAt                DateTime               @default(now()) @map("created_at")
  updatedAt                DateTime               @updatedAt @map("updated_at")

  @@map("poc_ingestion_state")
}

model PocIngestionRun {
  id                        String                @id @default(uuid()) @db.Uuid
  mode                      PocIngestionMode
  status                    PocIngestionRunStatus @default(RUNNING)
  startedAt                 DateTime              @default(now()) @map("started_at")
  completedAt               DateTime?             @map("completed_at")
  sourcesExpected           Int                   @default(0) @map("sources_expected")
  sourcesChecked            Int                   @default(0) @map("sources_checked")
  sourcesCollected          Int                   @default(0) @map("sources_collected")
  sourcesFailed             Int                   @default(0) @map("sources_failed")
  discoveryQueriesExpected  Int                   @default(0) @map("discovery_queries_expected")
  discoveryQueriesCompleted Int                   @default(0) @map("discovery_queries_completed")
  articlesDiscovered        Int                   @default(0) @map("articles_discovered")
  articlesProcessed         Int                   @default(0) @map("articles_processed")
  articlesFailed            Int                   @default(0) @map("articles_failed")
  duplicatesPrevented       Int                   @default(0) @map("duplicates_prevented")
  pendingBacklog            Int                   @default(0) @map("pending_backlog")
  errorCode                 String?               @map("error_code")
  errorMessage              String?               @map("error_message")
  createdAt                 DateTime              @default(now()) @map("created_at")

  @@index([startedAt])
  @@index([mode, status, completedAt])
  @@map("poc_ingestion_runs")
}

model CustomerMonitoringTag {
  id              String                @id @default(uuid()) @db.Uuid
  customerId      String                @map("customer_id") @db.Uuid
  type            MonitoringTagType
  key             String
  label           String
  normalizedLabel String                @map("normalized_label")
  category        MonitoringTagCategory
  status          MonitoringTagStatus
  reason          String
  derivedActive   Boolean               @default(true) @map("derived_active")
  createdByUserId String?               @map("created_by_user_id") @db.Uuid
  customer        Customer              @relation(fields: [customerId], references: [id], onDelete: Cascade)
  createdByUser   User?                 @relation("MonitoringTagCreator", fields: [createdByUserId], references: [id], onDelete: SetNull)
  createdAt       DateTime              @default(now()) @map("created_at")
  updatedAt       DateTime              @updatedAt @map("updated_at")

  @@unique([customerId, type, key])
  @@index([customerId, type, status, derivedActive])
  @@map("customer_monitoring_tags")
}

model CustomerSourcePreference {
  customerId  String   @map("customer_id") @db.Uuid
  sourceId    String   @map("source_id") @db.Uuid
  enabled     Boolean  @default(true)
  recommended Boolean  @default(false)
  reason      String?
  customer    Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
  source      Source   @relation(fields: [sourceId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  @@id([customerId, sourceId])
  @@index([sourceId, enabled])
  @@map("customer_source_preferences")
}

model SourceArticle {
  id                    String                      @id @default(uuid()) @db.Uuid
  sourceId              String                      @map("source_id") @db.Uuid
  source                Source                      @relation(fields: [sourceId], references: [id], onDelete: Restrict)
  originalUrl           String                      @map("original_url")
  canonicalUrl          String?                     @map("canonical_url")
  externalId            String?                     @map("external_id")
  title                 String
  author                String?
  publishedAt           DateTime?                   @map("published_at")
  discoveredAt          DateTime                    @default(now()) @map("discovered_at")
  collectedAt           DateTime                    @default(now()) @map("collected_at")
  language              String?
  country               String?
  region                String?
  category              SourceCategory?
  rawText               String?                     @map("raw_text")
  normalizedText        String?                     @map("normalized_text")
  excerpt               String?
  contentHash           String                      @map("content_hash")
  urlHash               String                      @map("url_hash")
  status                SourceArticleStatus         @default(COLLECTED)
  extractionRuns        ArticleExtractionRun[]
  extractionLease       ArticleExtractionLease?
  claims                Claim[]
  pocEvaluationArticles PocEvaluationArticle[]
  newsRadarProcessing   NewsRadarArticleProcessing?
  newsRadarExposures    NewsRadarExposure[]
  translations          SourceArticleTranslation[]
  createdAt             DateTime                    @default(now()) @map("created_at")
  updatedAt             DateTime                    @updatedAt @map("updated_at")

  @@unique([sourceId, externalId])
  @@unique([sourceId, urlHash])
  @@index([sourceId, contentHash])
  @@index([collectedAt, status])
  @@map("source_articles")
}

model NewsRadarArticleProcessing {
  sourceArticleId   String                    @id @map("source_article_id") @db.Uuid
  sourceArticle     SourceArticle             @relation(fields: [sourceArticleId], references: [id], onDelete: Cascade)
  status            NewsRadarProcessingStatus @default(RUNNING)
  policyVersion     String                    @map("policy_version")
  ownerToken        String                    @map("owner_token") @db.Uuid
  leaseExpiresAt    DateTime                  @map("lease_expires_at")
  attemptCount      Int                       @default(1) @map("attempt_count")
  topics            NewsRadarTopic[]
  detectedTerms     String[]                  @map("detected_terms")
  detectedLocations String[]                  @map("detected_locations")
  completedAt       DateTime?                 @map("completed_at")
  errorCode         String?                   @map("error_code")
  errorMessage      String?                   @map("error_message")
  createdAt         DateTime                  @default(now()) @map("created_at")
  updatedAt         DateTime                  @updatedAt @map("updated_at")

  @@index([status, leaseExpiresAt])
  @@map("news_radar_article_processing")
}

model NewsRadarExposure {
  id               String                  @id @default(uuid()) @db.Uuid
  customerId       String                  @map("customer_id") @db.Uuid
  sourceArticleId  String                  @map("source_article_id") @db.Uuid
  matchKey         String                  @map("match_key")
  policyVersion    String                  @map("policy_version")
  entityType       NewsRadarEntityType     @map("entity_type")
  topic            NewsRadarTopic
  matchMethod      NewsRadarMatchMethod    @map("match_method")
  confidence       Decimal                 @db.Decimal(4, 3)
  relevanceLevel   NewsRadarRelevanceLevel @map("relevance_level")
  reason           String
  matchedTerms     String[]                @map("matched_terms")
  pathSnapshot     Json                    @map("path_snapshot")
  supplierId       String?                 @map("supplier_id") @db.Uuid
  factoryId        String?                 @map("factory_id") @db.Uuid
  productId        String?                 @map("product_id") @db.Uuid
  materialId       String?                 @map("material_id") @db.Uuid
  routeId          String?                 @map("route_id") @db.Uuid
  routePortRouteId String?                 @map("route_port_route_id") @db.Uuid
  portId           String?                 @map("port_id") @db.Uuid
  customer         Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  sourceArticle    SourceArticle           @relation(fields: [sourceArticleId], references: [id], onDelete: Restrict)
  supplier         Supplier?               @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  factory          Factory?                @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  product          Product?                @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  material         Material?               @relation(fields: [materialId, customerId], references: [id, customerId], onDelete: Restrict)
  route            Route?                  @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  routePort        RoutePort?              @relation(fields: [routePortRouteId, portId, customerId], references: [routeId, portId, customerId], onDelete: Restrict)
  dailyBriefItems  DailyBriefItem[]
  createdAt        DateTime                @default(now()) @map("created_at")
  updatedAt        DateTime                @updatedAt @map("updated_at")

  @@unique([customerId, sourceArticleId, matchKey, policyVersion])
  @@unique([id, customerId])
  @@index([customerId, createdAt])
  @@index([sourceArticleId])
  @@index([customerId, entityType])
  @@map("news_radar_exposures")
}

model SourceArticleTranslation {
  id                String                   @id @default(uuid()) @db.Uuid
  sourceArticleId   String                   @map("source_article_id") @db.Uuid
  sourceArticle     SourceArticle            @relation(fields: [sourceArticleId], references: [id], onDelete: Cascade)
  targetLanguage    String                   @map("target_language")
  translatedTitle   String?                  @map("translated_title")
  translatedSummary String?                  @map("translated_summary")
  provider          String?
  model             String?
  status            ArticleTranslationStatus @default(PENDING)
  errorCode         String?                  @map("error_code")
  createdAt         DateTime                 @default(now()) @map("created_at")
  updatedAt         DateTime                 @updatedAt @map("updated_at")

  @@unique([sourceArticleId, targetLanguage])
  @@index([status, targetLanguage])
  @@map("source_article_translations")
}

model DailyBriefPreference {
  id           String               @id @default(uuid()) @db.Uuid
  userId       String               @map("user_id") @db.Uuid
  customerId   String               @map("customer_id") @db.Uuid
  enabled      Boolean              @default(false)
  deliveryTime String               @default("08:00") @map("delivery_time")
  timezone     String               @default("UTC")
  email        String
  language     String               @default("en")
  user         User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  customer     Customer             @relation(fields: [customerId], references: [id], onDelete: Cascade)
  membership   CustomerMembership   @relation(fields: [userId, customerId], references: [userId, customerId], onDelete: Cascade)
  deliveries   DailyBriefDelivery[]
  createdAt    DateTime             @default(now()) @map("created_at")
  updatedAt    DateTime             @updatedAt @map("updated_at")

  @@unique([userId, customerId])
  @@unique([id, customerId, userId])
  @@index([customerId, enabled])
  @@map("daily_brief_preferences")
}

model DailyBrief {
  id                  String               @id @default(uuid()) @db.Uuid
  customerId          String               @map("customer_id") @db.Uuid
  briefDate           DateTime             @map("brief_date") @db.Date
  generatedAt         DateTime             @default(now()) @map("generated_at")
  graphRevision       BigInt               @map("graph_revision")
  supplyChainSnapshot Json                 @map("supply_chain_snapshot")
  customer            Customer             @relation(fields: [customerId], references: [id], onDelete: Restrict)
  items               DailyBriefItem[]
  deliveries          DailyBriefDelivery[]
  createdAt           DateTime             @default(now()) @map("created_at")
  updatedAt           DateTime             @updatedAt @map("updated_at")

  @@unique([customerId, briefDate])
  @@unique([id, customerId])
  @@index([customerId, generatedAt])
  @@map("daily_briefs")
}

model DailyBriefDelivery {
  id           String                   @id @default(uuid()) @db.Uuid
  preferenceId String                   @map("preference_id") @db.Uuid
  briefId      String                   @map("brief_id") @db.Uuid
  customerId   String                   @map("customer_id") @db.Uuid
  userId       String                   @map("user_id") @db.Uuid
  email        String
  language     String
  status       DailyBriefDeliveryStatus @default(PENDING)
  provider     String?
  providerId   String?                  @map("provider_id")
  sentAt       DateTime?                @map("sent_at")
  errorCode    String?                  @map("error_code")
  preference   DailyBriefPreference     @relation(fields: [preferenceId, customerId, userId], references: [id, customerId, userId], onDelete: Restrict)
  brief        DailyBrief               @relation(fields: [briefId, customerId], references: [id, customerId], onDelete: Restrict)
  customer     Customer                 @relation(fields: [customerId], references: [id], onDelete: Restrict)
  user         User                     @relation(fields: [userId], references: [id], onDelete: Restrict)
  createdAt    DateTime                 @default(now()) @map("created_at")
  updatedAt    DateTime                 @updatedAt @map("updated_at")

  @@unique([preferenceId, briefId])
  @@index([customerId, status, createdAt])
  @@map("daily_brief_deliveries")
}

model DailyBriefItem {
  id         String            @id @default(uuid()) @db.Uuid
  customerId String            @map("customer_id") @db.Uuid
  briefId    String            @map("brief_id") @db.Uuid
  exposureId String            @map("exposure_id") @db.Uuid
  section    String
  position   Int
  brief      DailyBrief        @relation(fields: [briefId, customerId], references: [id, customerId], onDelete: Cascade)
  exposure   NewsRadarExposure @relation(fields: [exposureId, customerId], references: [id, customerId], onDelete: Restrict)
  createdAt  DateTime          @default(now()) @map("created_at")

  @@unique([briefId, exposureId])
  @@unique([briefId, position])
  @@index([customerId, exposureId])
  @@map("daily_brief_items")
}

model ArticleExtractionLease {
  sourceArticleId String        @id @map("source_article_id") @db.Uuid
  sourceArticle   SourceArticle @relation(fields: [sourceArticleId], references: [id], onDelete: Cascade)
  ownerToken      String        @map("owner_token") @db.Uuid
  leaseExpiresAt  DateTime      @map("lease_expires_at")
  acquiredAt      DateTime      @default(now()) @map("acquired_at")

  @@index([leaseExpiresAt])
  @@map("article_extraction_leases")
}

model ArticleExtractionRun {
  id                    String                 @id @default(uuid()) @db.Uuid
  sourceArticleId       String                 @map("source_article_id") @db.Uuid
  sourceArticle         SourceArticle          @relation(fields: [sourceArticleId], references: [id], onDelete: Restrict)
  status                ExtractionRunStatus    @default(PROCESSING)
  provider              String
  model                 String
  promptVersion         String                 @map("prompt_version")
  schemaVersion         String                 @map("schema_version")
  inputHash             String                 @map("input_hash")
  inputCharacters       Int                    @map("input_characters")
  inputTruncated        Boolean                @default(false) @map("input_truncated")
  inputTokens           Int?                   @map("input_tokens")
  outputTokens          Int?                   @map("output_tokens")
  claimsExtracted       Int                    @default(0) @map("claims_extracted")
  articleRelevant       Boolean?               @map("article_relevant")
  startedAt             DateTime               @default(now()) @map("started_at")
  completedAt           DateTime?              @map("completed_at")
  errorCode             String?                @map("error_code")
  errorMessage          String?                @map("error_message")
  claims                Claim[]
  pocEvaluationArticles PocEvaluationArticle[]
  createdAt             DateTime               @default(now()) @map("created_at")

  @@unique([id, sourceArticleId])
  @@index([sourceArticleId, startedAt])
  @@index([status, startedAt])
  @@map("article_extraction_runs")
}

model Claim {
  id                     String                  @id @default(uuid()) @db.Uuid
  sourceArticleId        String                  @map("source_article_id") @db.Uuid
  extractionRunId        String                  @map("extraction_run_id") @db.Uuid
  sourceArticle          SourceArticle           @relation(fields: [sourceArticleId], references: [id], onDelete: Restrict)
  extractionRun          ArticleExtractionRun    @relation(fields: [extractionRunId], references: [id], onDelete: Restrict)
  claimType              ClaimType               @map("claim_type")
  assertionMode          AssertionMode           @map("assertion_mode")
  statement              String
  confidence             Decimal                 @db.Decimal(4, 3)
  occurredAt             DateTime?               @map("occurred_at")
  validFrom              DateTime?               @map("valid_from")
  validUntil             DateTime?               @map("valid_until")
  evidenceText           String                  @map("evidence_text")
  evidenceStart          Int                     @map("evidence_start")
  evidenceEnd            Int                     @map("evidence_end")
  entities               ClaimEntity[]
  locations              ClaimLocation[]
  evaluations            ClaimEvaluation[]
  eventLinks             EventClaim[]
  eventProcessing        ClaimEventProcessing?
  eventEntityIdentifiers EventEntityIdentifier[]
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")

  @@index([sourceArticleId, createdAt])
  @@index([extractionRunId])
  @@index([claimType, occurredAt])
  @@map("claims")
}

model Event {
  id                     String                 @id @default(uuid()) @db.Uuid
  eventType              EventType              @map("event_type")
  status                 EventStatus            @default(DETECTED)
  title                  String
  summary                String
  severity               EventSeverity
  confidence             Decimal                @db.Decimal(4, 3)
  assertionMode          AssertionMode          @map("assertion_mode")
  startDate              DateTime?              @map("start_date")
  endDate                DateTime?              @map("end_date")
  occurredAt             DateTime?              @map("occurred_at")
  observedAt             DateTime?              @map("observed_at")
  temporalPrecision      EventTemporalPrecision @default(UNKNOWN) @map("temporal_precision")
  firstSeenAt            DateTime               @map("first_seen_at")
  lastSeenAt             DateTime               @map("last_seen_at")
  fingerprint            String                 @unique
  policyVersion          String                 @default("1.1") @map("policy_version")
  conflictState          EventConflictState     @default(NONE) @map("conflict_state")
  conflictReason         String?                @map("conflict_reason")
  supportingClaimCount   Int                    @default(0) @map("supporting_claim_count")
  supportingArticleCount Int                    @default(0) @map("supporting_article_count")
  supportingSourceCount  Int                    @default(0) @map("supporting_source_count")
  claimLinks             EventClaim[]
  entities               EventEntity[]
  locations              EventLocation[]
  exposureVersion        Int                    @default(1) @map("exposure_version")
  customerExposures      CustomerExposure[]
  exposureCandidates     ExposureCandidate[]
  createdAt              DateTime               @default(now()) @map("created_at")
  updatedAt              DateTime               @updatedAt @map("updated_at")

  @@index([eventType, status, lastSeenAt])
  @@index([severity, lastSeenAt])
  @@index([assertionMode, startDate])
  @@map("events")
}

model EventClaim {
  eventId       String             @map("event_id") @db.Uuid
  claimId       String             @map("claim_id") @db.Uuid
  event         Event              @relation(fields: [eventId], references: [id], onDelete: Restrict)
  claim         Claim              @relation(fields: [claimId], references: [id], onDelete: Restrict)
  matchDecision EventMatchDecision @map("match_decision")
  matchReason   String             @map("match_reason")
  claimSignal   ClaimSignal        @default(AFFIRMS_EVENT) @map("claim_signal")
  attachedAt    DateTime           @default(now()) @map("attached_at")

  @@id([eventId, claimId])
  @@index([claimId])
  @@index([eventId])
  @@map("event_claims")
}

model EventEntity {
  id             String                  @id @default(uuid()) @db.Uuid
  eventId        String                  @map("event_id") @db.Uuid
  event          Event                   @relation(fields: [eventId], references: [id], onDelete: Cascade)
  entityType     EventEntityType         @map("entity_type")
  name           String
  normalizedName String                  @map("normalized_name")
  normalizedKey  String                  @map("normalized_key")
  role           String?
  identifiers    EventEntityIdentifier[]
  createdAt      DateTime                @default(now()) @map("created_at")

  @@unique([eventId, normalizedKey])
  @@index([entityType, normalizedName])
  @@map("event_entities")
}

model EventEntityIdentifier {
  id                   String                     @id @default(uuid()) @db.Uuid
  eventEntityId        String                     @map("event_entity_id") @db.Uuid
  namespace            String
  identifier           String
  normalizedIdentifier String                     @map("normalized_identifier")
  verificationStatus   IdentityVerificationStatus @default(UNVERIFIED) @map("verification_status")
  provenanceSource     String                     @map("provenance_source")
  provenanceRef        String?                    @map("provenance_ref")
  evidenceNote         String?                    @map("evidence_note")
  sourceClaimId        String?                    @map("source_claim_id") @db.Uuid
  proposedAt           DateTime?                  @map("proposed_at")
  proposedByUserId     String?                    @map("proposed_by_user_id") @db.Uuid
  verifiedAt           DateTime?                  @map("verified_at")
  verifiedByUserId     String?                    @map("verified_by_user_id") @db.Uuid
  eventEntity          EventEntity                @relation(fields: [eventEntityId], references: [id], onDelete: Cascade)
  sourceClaim          Claim?                     @relation(fields: [sourceClaimId], references: [id], onDelete: Restrict)
  proposedBy           User?                      @relation("EventEntityIdentifierProposer", fields: [proposedByUserId], references: [id], onDelete: Restrict)
  verifiedBy           User?                      @relation("EventEntityIdentifierVerifier", fields: [verifiedByUserId], references: [id], onDelete: Restrict)
  createdAt            DateTime                   @default(now()) @map("created_at")
  updatedAt            DateTime                   @updatedAt @map("updated_at")

  @@unique([eventEntityId, namespace, normalizedIdentifier])
  @@index([namespace, normalizedIdentifier, verificationStatus])
  @@index([sourceClaimId])
  @@map("event_entity_identifiers")
}

model EventLocation {
  id            String            @id @default(uuid()) @db.Uuid
  eventId       String            @map("event_id") @db.Uuid
  event         Event             @relation(fields: [eventId], references: [id], onDelete: Cascade)
  locationType  EventLocationType @default(OTHER) @map("location_type")
  name          String
  country       String?
  region        String?
  city          String?
  latitude      Decimal?          @db.Decimal(9, 6)
  longitude     Decimal?          @db.Decimal(9, 6)
  normalizedKey String            @map("normalized_key")
  createdAt     DateTime          @default(now()) @map("created_at")

  @@unique([eventId, normalizedKey])
  @@index([country, region, city])
  @@map("event_locations")
}

model CustomerExposure {
  id                    String              @id @default(uuid()) @db.Uuid
  customerId            String              @map("customer_id") @db.Uuid
  eventId               String              @map("event_id") @db.Uuid
  status                ExposureStatus      @default(POTENTIAL)
  primaryExposureType   ExposureType        @map("primary_exposure_type")
  matchConfidence       Decimal             @map("match_confidence") @db.Decimal(4, 3)
  matchState            ExposureMatchState  @map("match_state")
  eventPolicyVersion    String              @map("event_policy_version")
  exposurePolicyVersion String              @map("exposure_policy_version")
  eventVersion          Int                 @map("event_version")
  graphRevision         BigInt              @map("graph_revision")
  firstDetectedAt       DateTime            @default(now()) @map("first_detected_at")
  lastMatchedAt         DateTime            @map("last_matched_at")
  resolvedAt            DateTime?           @map("resolved_at")
  staleAt               DateTime?           @map("stale_at")
  dismissedAt           DateTime?           @map("dismissed_at")
  reviewedByUserId      String?             @map("reviewed_by_user_id") @db.Uuid
  reviewReasonCode      ExposureReasonCode? @map("review_reason_code")
  customer              Customer            @relation(fields: [customerId], references: [id], onDelete: Restrict)
  event                 Event               @relation(fields: [eventId], references: [id], onDelete: Restrict)
  reviewedBy            User?               @relation(fields: [reviewedByUserId], references: [id], onDelete: Restrict)
  paths                 ExposurePath[]
  createdAt             DateTime            @default(now()) @map("created_at")
  updatedAt             DateTime            @updatedAt @map("updated_at")

  @@unique([customerId, eventId])
  @@unique([id, customerId])
  @@index([customerId, status, lastMatchedAt])
  @@index([customerId, primaryExposureType, status])
  @@index([eventId, status])
  @@map("customer_exposures")
}

model ExposurePath {
  id              String               @id @default(uuid()) @db.Uuid
  customerId      String               @map("customer_id") @db.Uuid
  exposureId      String               @map("exposure_id") @db.Uuid
  pathKey         String               @map("path_key")
  exposureType    ExposureType         @map("exposure_type")
  decision        ExposureDecision
  matchMethod     ExposureMatchMethod  @map("match_method")
  matchConfidence Decimal              @map("match_confidence") @db.Decimal(4, 3)
  reasonCodes     ExposureReasonCode[] @map("reason_codes")
  activeMatch     Boolean              @default(true) @map("active_match")
  firstMatchedAt  DateTime             @default(now()) @map("first_matched_at")
  lastMatchedAt   DateTime             @map("last_matched_at")
  invalidatedAt   DateTime?            @map("invalidated_at")
  graphRevision   BigInt               @map("graph_revision")
  eventVersion    Int                  @map("event_version")
  snapshot        Json
  exposure        CustomerExposure     @relation(fields: [exposureId, customerId], references: [id, customerId], onDelete: Restrict)
  steps           ExposurePathStep[]
  createdAt       DateTime             @default(now()) @map("created_at")
  updatedAt       DateTime             @updatedAt @map("updated_at")

  @@unique([exposureId, pathKey])
  @@unique([id, customerId])
  @@index([customerId, activeMatch, exposureType])
  @@index([exposureId, decision])
  @@map("exposure_paths")
}

model ExposurePathStep {
  id                 String           @id @default(uuid()) @db.Uuid
  customerId         String           @map("customer_id") @db.Uuid
  pathId             String           @map("path_id") @db.Uuid
  sequence           Int
  nodeType           ExposureNodeType @map("node_type")
  edgeFromPrevious   String?          @map("edge_from_previous")
  supplierId         String?          @map("supplier_id") @db.Uuid
  factoryId          String?          @map("factory_id") @db.Uuid
  productId          String?          @map("product_id") @db.Uuid
  materialId         String?          @map("material_id") @db.Uuid
  routeId            String?          @map("route_id") @db.Uuid
  portId             String?          @map("port_id") @db.Uuid
  labelSnapshot      String           @map("label_snapshot")
  attributesSnapshot Json?            @map("attributes_snapshot")
  activeSnapshot     Boolean?         @map("active_snapshot")
  path               ExposurePath     @relation(fields: [pathId, customerId], references: [id, customerId], onDelete: Restrict)
  supplier           Supplier?        @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  factory            Factory?         @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  product            Product?         @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  material           Material?        @relation(fields: [materialId, customerId], references: [id, customerId], onDelete: Restrict)
  route              Route?           @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  port               Port?            @relation(fields: [portId], references: [id], onDelete: Restrict)
  createdAt          DateTime         @default(now()) @map("created_at")

  @@unique([pathId, sequence])
  @@index([customerId, supplierId])
  @@index([customerId, factoryId])
  @@index([customerId, productId])
  @@index([customerId, materialId])
  @@index([customerId, routeId])
  @@index([portId])
  @@map("exposure_path_steps")
}

model CustomerGraphIdentity {
  id                   String                     @id @default(uuid()) @db.Uuid
  customerId           String                     @map("customer_id") @db.Uuid
  subjectType          IdentitySubjectType        @map("subject_type")
  supplierId           String?                    @map("supplier_id") @db.Uuid
  factoryId            String?                    @map("factory_id") @db.Uuid
  productId            String?                    @map("product_id") @db.Uuid
  materialId           String?                    @map("material_id") @db.Uuid
  routeId              String?                    @map("route_id") @db.Uuid
  portId               String?                    @map("port_id") @db.Uuid
  namespace            String
  identifier           String
  normalizedIdentifier String                     @map("normalized_identifier")
  verificationStatus   IdentityVerificationStatus @default(UNVERIFIED) @map("verification_status")
  provenanceSource     String                     @map("provenance_source")
  provenanceRef        String?                    @map("provenance_ref")
  evidenceNote         String?                    @map("evidence_note")
  verifiedAt           DateTime?                  @map("verified_at")
  verifiedByUserId     String?                    @map("verified_by_user_id") @db.Uuid
  customer             Customer                   @relation(fields: [customerId], references: [id], onDelete: Restrict)
  supplier             Supplier?                  @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  factory              Factory?                   @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  product              Product?                   @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  material             Material?                  @relation(fields: [materialId, customerId], references: [id, customerId], onDelete: Restrict)
  route                Route?                     @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  port                 Port?                      @relation(fields: [portId], references: [id], onDelete: Restrict)
  verifiedBy           User?                      @relation(fields: [verifiedByUserId], references: [id], onDelete: Restrict)
  resultingCandidates  ExposureCandidate[]
  createdAt            DateTime                   @default(now()) @map("created_at")
  updatedAt            DateTime                   @updatedAt @map("updated_at")

  @@unique([id, customerId])
  @@index([namespace, normalizedIdentifier, verificationStatus])
  @@index([customerId, supplierId])
  @@index([customerId, factoryId])
  @@index([customerId, productId])
  @@index([customerId, materialId])
  @@index([customerId, routeId])
  @@index([portId])
  @@map("customer_graph_identities")
}

model ExposureCandidate {
  id                    String                  @id @default(uuid()) @db.Uuid
  customerId            String                  @map("customer_id") @db.Uuid
  eventId               String                  @map("event_id") @db.Uuid
  candidateKey          String                  @map("candidate_key")
  status                ExposureCandidateStatus @default(PENDING)
  confidence            Decimal?                @db.Decimal(4, 3)
  matchMethods          ExposureMatchMethod[]   @map("match_methods")
  reasonCodes           ExposureReasonCode[]    @map("reason_codes")
  eventEntityIds        String[]                @map("event_entity_ids") @db.Uuid
  eventLocationIds      String[]                @map("event_location_ids") @db.Uuid
  snapshot              Json
  exposurePolicyVersion String                  @map("exposure_policy_version")
  graphRevision         BigInt                  @map("graph_revision")
  eventVersion          Int                     @map("event_version")
  reviewedAt            DateTime?               @map("reviewed_at")
  reviewedByUserId      String?                 @map("reviewed_by_user_id") @db.Uuid
  reviewReasonCode      ExposureReasonCode?     @map("review_reason_code")
  resultingIdentityId   String?                 @map("resulting_identity_id") @db.Uuid
  customer              Customer                @relation(fields: [customerId], references: [id], onDelete: Restrict)
  event                 Event                   @relation(fields: [eventId], references: [id], onDelete: Restrict)
  reviewedBy            User?                   @relation(fields: [reviewedByUserId], references: [id], onDelete: Restrict)
  resultingIdentity     CustomerGraphIdentity?  @relation(fields: [resultingIdentityId, customerId], references: [id, customerId], onDelete: Restrict)
  nodes                 ExposureCandidateNode[]
  createdAt             DateTime                @default(now()) @map("created_at")
  updatedAt             DateTime                @updatedAt @map("updated_at")

  @@unique([customerId, eventId, candidateKey, exposurePolicyVersion])
  @@unique([id, customerId])
  @@index([customerId, status, createdAt])
  @@index([eventId, status])
  @@index([resultingIdentityId, customerId])
  @@map("exposure_candidates")
}

model ExposureCandidateNode {
  id          String            @id @default(uuid()) @db.Uuid
  customerId  String            @map("customer_id") @db.Uuid
  candidateId String            @map("candidate_id") @db.Uuid
  nodeType    ExposureNodeType  @map("node_type")
  supplierId  String?           @map("supplier_id") @db.Uuid
  factoryId   String?           @map("factory_id") @db.Uuid
  productId   String?           @map("product_id") @db.Uuid
  materialId  String?           @map("material_id") @db.Uuid
  routeId     String?           @map("route_id") @db.Uuid
  portId      String?           @map("port_id") @db.Uuid
  snapshot    Json
  candidate   ExposureCandidate @relation(fields: [candidateId, customerId], references: [id, customerId], onDelete: Restrict)
  supplier    Supplier?         @relation(fields: [supplierId, customerId], references: [id, customerId], onDelete: Restrict)
  factory     Factory?          @relation(fields: [factoryId, customerId], references: [id, customerId], onDelete: Restrict)
  product     Product?          @relation(fields: [productId, customerId], references: [id, customerId], onDelete: Restrict)
  material    Material?         @relation(fields: [materialId, customerId], references: [id, customerId], onDelete: Restrict)
  route       Route?            @relation(fields: [routeId, customerId], references: [id, customerId], onDelete: Restrict)
  port        Port?             @relation(fields: [portId], references: [id], onDelete: Restrict)
  createdAt   DateTime          @default(now()) @map("created_at")

  @@index([candidateId, customerId])
  @@index([customerId, supplierId])
  @@index([customerId, factoryId])
  @@index([customerId, productId])
  @@index([customerId, materialId])
  @@index([customerId, routeId])
  @@index([portId])
  @@map("exposure_candidate_nodes")
}

model ClaimEventProcessing {
  claimId           String                     @id @map("claim_id") @db.Uuid
  claim             Claim                      @relation(fields: [claimId], references: [id], onDelete: Cascade)
  status            ClaimEventProcessingStatus @default(PENDING)
  ownerToken        String?                    @map("owner_token") @db.Uuid
  leaseExpiresAt    DateTime?                  @map("lease_expires_at")
  attempts          Int                        @default(0)
  processedAt       DateTime?                  @map("processed_at")
  nextAttemptAt     DateTime?                  @map("next_attempt_at")
  errorCode         String?                    @map("error_code")
  errorMessage      String?                    @map("error_message")
  matchDecision     EventMatchDecision?        @map("match_decision")
  candidateEventIds Json?                      @map("candidate_event_ids")
  createdAt         DateTime                   @default(now()) @map("created_at")
  updatedAt         DateTime                   @updatedAt @map("updated_at")

  @@index([status, nextAttemptAt])
  @@index([leaseExpiresAt])
  @@map("claim_event_processing")
}

model PocEvaluationDataset {
  id                        String                 @id @default(uuid()) @db.Uuid
  name                      String
  description               String?
  status                    PocDatasetStatus       @default(DRAFT)
  provider                  String?
  model                     String?
  promptVersion             String?                @map("prompt_version")
  schemaVersion             String?                @map("schema_version")
  thresholds                Json
  minimumArticlesReviewed   Int                    @default(100) @map("minimum_articles_reviewed")
  minimumClaimReviewPercent Decimal                @default(90) @map("minimum_claim_review_percent") @db.Decimal(5, 2)
  articles                  PocEvaluationArticle[]
  claimEvaluations          ClaimEvaluation[]
  createdAt                 DateTime               @default(now()) @map("created_at")
  updatedAt                 DateTime               @updatedAt @map("updated_at")

  @@index([status, createdAt])
  @@map("poc_evaluation_datasets")
}

model PocEvaluationArticle {
  id              String                @id @default(uuid()) @db.Uuid
  datasetId       String                @map("dataset_id") @db.Uuid
  sourceArticleId String                @map("source_article_id") @db.Uuid
  extractionRunId String?               @map("extraction_run_id") @db.Uuid
  status          PocArticleStatus      @default(PENDING)
  errorCode       String?               @map("error_code")
  errorMessage    String?               @map("error_message")
  dataset         PocEvaluationDataset  @relation(fields: [datasetId], references: [id], onDelete: Restrict)
  sourceArticle   SourceArticle         @relation(fields: [sourceArticleId], references: [id], onDelete: Restrict)
  extractionRun   ArticleExtractionRun? @relation(fields: [extractionRunId, sourceArticleId], references: [id, sourceArticleId], onDelete: Restrict)
  evaluation      ArticleEvaluation?
  createdAt       DateTime              @default(now()) @map("created_at")
  updatedAt       DateTime              @updatedAt @map("updated_at")

  @@unique([datasetId, sourceArticleId])
  @@index([datasetId, status])
  @@index([extractionRunId])
  @@map("poc_evaluation_articles")
}

model ClaimEvaluation {
  id             String                   @id @default(uuid()) @db.Uuid
  datasetId      String                   @map("dataset_id") @db.Uuid
  claimId        String                   @map("claim_id") @db.Uuid
  reviewerUserId String                   @map("reviewer_user_id") @db.Uuid
  claimScore     EvaluationScore          @map("claim_score")
  evidenceScore  EvaluationScore          @map("evidence_score")
  entityScore    EvaluationScore          @map("entity_score")
  locationScore  EvaluationScore          @map("location_score")
  dateScore      EvaluationScore          @map("date_score")
  assertionScore EvaluationScore          @map("assertion_score")
  unsupported    Boolean                  @default(false)
  failureReason  EvaluationFailureReason? @map("failure_reason")
  notes          String?
  dataset        PocEvaluationDataset     @relation(fields: [datasetId], references: [id], onDelete: Restrict)
  claim          Claim                    @relation(fields: [claimId], references: [id], onDelete: Restrict)
  reviewer       User                     @relation(fields: [reviewerUserId], references: [id], onDelete: Restrict)
  createdAt      DateTime                 @default(now()) @map("created_at")
  updatedAt      DateTime                 @updatedAt @map("updated_at")

  @@unique([datasetId, claimId])
  @@index([reviewerUserId])
  @@map("claim_evaluations")
}

model ArticleEvaluation {
  id                String               @id @default(uuid()) @db.Uuid
  datasetArticleId  String               @unique @map("dataset_article_id") @db.Uuid
  reviewerUserId    String               @map("reviewer_user_id") @db.Uuid
  expectedRelevant  Boolean              @map("expected_relevant")
  extractedRelevant Boolean?             @map("extracted_relevant")
  missedClaims      Int                  @default(0) @map("missed_claims")
  missedClaimNotes  String?              @map("missed_claim_notes")
  notes             String?
  datasetArticle    PocEvaluationArticle @relation(fields: [datasetArticleId], references: [id], onDelete: Restrict)
  reviewer          User                 @relation(fields: [reviewerUserId], references: [id], onDelete: Restrict)
  createdAt         DateTime             @default(now()) @map("created_at")
  updatedAt         DateTime             @updatedAt @map("updated_at")

  @@index([reviewerUserId])
  @@map("article_evaluations")
}

model ClaimEntity {
  id             String          @id @default(uuid()) @db.Uuid
  claimId        String          @map("claim_id") @db.Uuid
  claim          Claim           @relation(fields: [claimId], references: [id], onDelete: Cascade)
  entityType     ClaimEntityType @map("entity_type")
  name           String
  normalizedName String?         @map("normalized_name")
  role           String?
  confidence     Decimal?        @db.Decimal(4, 3)
  createdAt      DateTime        @default(now()) @map("created_at")

  @@index([claimId])
  @@index([entityType, name])
  @@map("claim_entities")
}

model ClaimLocation {
  id         String   @id @default(uuid()) @db.Uuid
  claimId    String   @map("claim_id") @db.Uuid
  claim      Claim    @relation(fields: [claimId], references: [id], onDelete: Cascade)
  name       String
  country    String?
  region     String?
  city       String?
  confidence Decimal? @db.Decimal(4, 3)
  createdAt  DateTime @default(now()) @map("created_at")

  @@index([claimId])
  @@index([country, name])
  @@map("claim_locations")
}

model SourceCollectionRun {
  id              String              @id @default(uuid()) @db.Uuid
  sourceId        String              @map("source_id") @db.Uuid
  source          Source              @relation(fields: [sourceId], references: [id], onDelete: Restrict)
  collectorType   SourceType          @map("collector_type")
  startedAt       DateTime            @default(now()) @map("started_at")
  completedAt     DateTime?           @map("completed_at")
  status          CollectionRunStatus @default(RUNNING)
  itemsDiscovered Int                 @default(0) @map("items_discovered")
  itemsCreated    Int                 @default(0) @map("items_created")
  itemsUpdated    Int                 @default(0) @map("items_updated")
  itemsSkipped    Int                 @default(0) @map("items_skipped")
  itemsFailed     Int                 @default(0) @map("items_failed")
  errorCode       String?             @map("error_code")
  errorMessage    String?             @map("error_message")
  createdAt       DateTime            @default(now()) @map("created_at")

  @@index([sourceId, startedAt])
  @@map("source_collection_runs")
}
